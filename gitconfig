[user]
	name = Andrew Barchuk
	email = raindev@icloud.com
[core]
	editor = vim
	excludesfile = ~/.gitignore_global
[credential]
	helper = osxkeychain
[include]
	path = .gitconfig.local
[push]
	default = simple
[color]
	ui = true
[alias]
	blame = blame -wM
	pull = pull --ff-only
  update = !git fetch origin master && git stash && git rebase origin/master && git stash pop
  pll = !git stash && git pull && git stash pop
[filter "media"]
	clean = git-media-clean %f
	smudge = git-media-smudge %f
